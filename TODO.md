# TODO

## Текущий бэклог
- Airpay, 26.09 около 07:34 МСК: по явной команде владельца оплата разрешена только в отдельном Airpay Hub после повторной проверки пустой очереди и отсутствия неопределённых оплат. CRM видит `payments_enabled=true`; пересоздан только Airpay API. Следующий шаг — свежая подготовка и ручная покупка одной позиции владельцем, затем read-only сверка результата. Агент pay/reconcile/voucher не выполнял. Эта запись обновляет более ранний статус выключенной оплаты ниже.
- Airpay, актуально 26.09.2026: production CRM → отдельный Hub развёрнут 25.09, обе БД мигрированы, пользователь подтвердил UI/check. За 21,5 часа ошибок Airpay/Interhub, рестартов и OOM не найдено; очередь пуста, pay/voucher = 0, оплата выключена. Следующий шаг — отдельно разрешённое включение только Airpay и первая ручная покупка владельцем со свежим check, затем read-only сверка журнала. [Текущий статус](docs/airpay-rollout-plan.md#текущий-статус-после-выкладки-26092026) имеет приоритет над историческими записями этапов ниже. Внешний мониторинг и учебное восстановление БД/ключа ещё не выполнены.
- Эксплуатация CRM: разобрать частую буферизацию nginx во временные файлы (446 предупреждений за первые 21,5 часа, преимущественно `/api/payments/tbank/sbp` и `/api/accounts`). Сначала измерить размер ответов и эффект пагинации/буферов; не менять Interhub ради этого. Ошибок ответов Airpay с этими предупреждениями не выявлено.
- Поставщики, целевое решение: CRM/Seller → единый логический Supplier Hub → адаптер Interhub/Airpay; изоляцию процессов и таблиц можно сохранить. [Актуальная последовательность и ближайший шаг](docs/airpay-rollout-plan.md#актуальный-ориентир-и-ближайший-шаг-25092026): сейчас готовность хоста и изолированное внедрение Airpay в «Платежах» с выключенной оплатой; затем сделки/Seller и общий интерфейс/история. Перенос прямого Interhub из CRM — только отдельный будущий согласованный проект. Никаких реальных pay/reconcile/получений ваучера агентом.
- Airpay, второй шаг реализован: атомарный журнал событий, остановка повторов после пяти незавершённых запросов, защищённое ручное решение без транспорта поставщика, UI журнала/разбора. Миграции 05/06 применены только к staging CRM, DB-smoke пройдены с подменённым поставщиком, Hub runtime синхронизирован. Поиск/фильтры/Excel и восстановление очереди завершены; далее — отдельное развёртывание Airpay Hub; реальные покупки выполняет только пользователь.
- Airpay, первый шаг выполнен в коде: [контракт airpay.v1](docs/airpay-contract.md), проверка совместимости Hub до POST, отдельные состояния оплаты/результата, безопасная карточка без кода, глобальная диагностика перехода и блокировка новых действий при paid-ваучерах без кода. Окружения не переключены; первый шаг не менял схему БД.
- Airpay, приоритетный этап: корректная ручная покупка из «Платежей» через единственного исполнителя Supplier Hub. [Зафиксированный аудит и план A–E](docs/airpay-rollout-plan.md): контракт Airpay; оплата отдельно от готовности кода; операторский разбор и журнал событий; история/UI; барьер перехода для paid-ваучеров без кода; восстановление, изолированные проверки и развёртывание с выключенной оплатой. Существующий Interhub в трёх проектах не менять. Реальные pay/reconcile/выдачу выполняет только пользователь.
- Airpay, следующие этапы после «Платежей»: подключение к сделкам (связь, код, себестоимость без перезаписи сумм Interhub), Seller (явный выбор поставщика, строковый service_id, неизменяемый снимок покупки, workspace/магазин, лимит цены и валюта, запрет fallback при неизвестном исходе), общая история поставщиков. Нельзя просто добавить Airpay в связки Seller: текущий клиент отправляет provider_code=interhub. Смешанные закупки в сделке требуют отдельного решения по учёту.
- Airpay: очередь, переоценка неоплаченного остатка, отдельное раскрытие/шифрование кодов и изолированный модуль Supplier Hub реализованы; миграции CRM 01–04 применены к staging, DB smoke с конкурирующими соединениями и подменённым поставщиком пройден. Развёртывание нового модуля Hub и переключение AIRPAY_BACKEND выполняются отдельно по [инструкции](docs/airpay.md), без изменения Interhub и без двух исполнителей. Реальные pay/выдачу проверяет только пользователь. До расширения типов услуг подтвердить у Airpay часовой пояс agentTransactionDate, ev_* → extras и формат договоров/квитанций (их pay заблокирован). Интеграция в сделки/селлер остаётся за пределами текущего окна «Платежи».
- Селлер → каталог ваучеров (отдельный этап): выбирать собственный номинал товара и читать включённые связки по `fulfillment_priority`; переходить к следующему только при отсутствии остатка или однозначном отказе без покупки. При неопределённом результате покупки сначала восстанавливать её статус у того же поставщика с идемпотентным ключом; после исчерпания списка использовать существующую ручную выдачу. Текущая реализация хранит только настройки каталога и не меняет селлер.
- Каталог ваучеров: для больших пакетов номиналов добавить долговечную очередь с прогрессом первичной загрузки цен. Сейчас пакет до 100 номиналов сохраняется атомарно после последовательного calculate; длительность зависит от Интерхаба и может превысить таймаут внешнего reverse-proxy.
- RBAC: убрать исторический DDL из `ensure_rbac_schema_and_defaults` в `api/domains/rbac_api.py`; схема и начальные данные должны полностью готовиться мигратором. Новый раздел «Каталог» уже добавляется runtime-миграцией.
- Каталог ваучеров: при росте числа связанных номиналов вынести обходы из lifespan API в отдельный worker с отменой при остановке и ограничением частоты запросов между всеми интеграциями Интерхаба. Сейчас расписание и защита от параллельных запусков хранятся в PostgreSQL.
- InterHub: перенести фоновые обновления цен/остатков и их прогресс из памяти процесса в долговечную очередь; текущая защита от повторного запуска действует только внутри одного API worker, а перезапуск теряет job_id.
- InterHub: согласовать явную таблицу соответствий для разных названий одного номинала (например, PNL/PLN и префиксы gf_). В кэше приложения несовпадения и дубли остаются неизвестными, без автоматического угадывания только по цифрам.
- История покупок CRM в таблице: оптимизировать постраничные запросы с повторным подсчётом и поиском последних цен. Выгрузка Excel уже использует отдельное чтение одним запросом; учитывать сохранение поиска и сортировки при переносе оптимизации в таблицу.
- Перед возвратом виджета TR-карты исключить рублёвые покупки ваучеров из списаний с карты в TRY: `_load_finance_card_balance` сейчас вычитает закуп всех завершённых услуг TR без различения способа оплаты.
- Проверить по аудиту старые услуги TR/PL без оплаченных ваучеров, сохранённые после релиза `77b62cd`: фронтенд мог передать закуп 0. Исправление сохранения подготовлено; production-проверку и возможное восстановление сумм из аудита/резервной копии согласовать отдельно, не заменять нулевые суммы массово.
- Безопасность: WebSocket `/ws/deals` передаёт токен в query string, который попадает в access-log Uvicorn. Убрать токены из логов и пересмотреть передачу авторизации; до исправления ограничить доступ к логам и не включать сырые строки WebSocket в диагностику.
- Разделить крупный JS-бандл фронта: `npm run check` предупреждает о чанке больше 500 КБ после минификации; проверить ленивую загрузку разделов WorkView.
- Баланс TR-карты временно скрыт из шапки по запросу пользователя. Когда функционал снова станет актуален, вернуть подключение `WorkTrCardBalanceWidget` в `WorkTopBar`; компонент, расчёты и тесты сохранены.
- Поставщик/сделки: дополнить изолированные API-тесты проверкой настоящих конкурирующих транзакций PostgreSQL (pay, сохранение, удаление, фоновое подтверждение) на отдельной локальной тестовой БД; внешний API поставщика подменять, реальные ваучеры не покупать.
- Добавить пароль и ограничить доступ к Redis (`requirepass`, `bind/network rules`, обновить `REDIS_URL`).
- Добавить серверный поиск/ленивую загрузку аккаунтов в селекторах сделок, чтобы не грузить все аккаунты сразу.
- Добавить частичный индекс активных слотов для ускорения `/accounts/for-deal/availability` (`WHERE released_at IS NULL`).
- Исправить лимит `loadAccountDeals`: пагинация/"Показать еще" или явное уведомление "первые N".
- Защититься от гонки при одновременном создании шеринга: уникальный индекс активного слота (`account_id + slot_type_code WHERE released_at IS NULL`) + обработка 409 на API и сообщение на фронте.
- Добавить CI (GitHub Actions): автоматический запуск `cd gamesales-web && npm run check` на каждый PR.
- Добавить PR-шаблон (`.github/pull_request_template.md`) с чеклистом: комментарии, тесты, локальные проверки.
- Убрать `game-only` ограничения в API аккаунтов для слотов (`/accounts/for-deal`, `/accounts/for-deal/availability`, `/accounts/{id}/products`) и перевести их на поддержку `subscription` без фронтовых fallback-веток.
- Добавить backend-тесты на сценарии шеринга с `subscription` для API аккаунтов (поиск аккаунтов/доступность слотов/привязка товара).
- Для резервов в шеринге: освобождать `reserve_key` при удалении/отмене сделки (сейчас резерв помечается занятым сразу при создании).
- Для резервов в шеринге: добавить ручной выбор/переопределение резерва в форме сделки с серверной валидацией уникальности.
- Централизовать инвалидацию/рефреш клиентских кешей после мутаций (create/update/delete/import): `accountSecrets`, `accountsAll`, `productsAll`, справочники; убрать точечные рассинхроны UI без ручной кнопки "Обновить".
- Для синхронизации Yandex Market: обнулять или отменять старый дневной итог, если при повторной загрузке за диапазон день полностью исчез из доставленных строк отчета.
- Для уведомлений Yandex Market: вывести журнал и причины повторов в интерфейсе владельца; после анализа боевой истории определить политику дедупликации по `event_fingerprint`.
- Для повторной публикации остатка Yandex Market после выдачи: заменить краткоживущий `Timer` на долговечную очередь с повторами.
- Для ручной выдачи ключей Ozon: вынести хранение уже отправленных ключей из plaintext jsonb в отдельное шифруемое хранилище с аудитом доступа до подключения массовых продаж.

- Airpay: при отдельном развёртывании подключить внешний мониторинг /live, /ready и очереди, проверить резервное восстановление БД вместе с ключом шифрования. Встроенная диагностика очереди — снимок DB-locks, не распределённый heartbeat; при пустой очереди она не подтверждает живой worker. Порядок переключения: [airpay-operations.md](docs/airpay-operations.md).

- Airpay, защита выката 25.09: ошибки запуска изолированы от CRM, UI загружается отдельно, Compose самостоятельный с отдельным env и выключенной оплатой, есть preflight и сборщик исходной поставки с SHA256. Перед production выполнить Docker build/preflight на выбранном хосте (локально Docker отсутствует), проверить резервное восстановление и реальные env/ревизии/ресурсы, список миграций и ручной UI smoke. Автотесты не заменяют эти проверки.

## Архив
- Для услуг TR/PL со связанной покупкой InterHub в статусе `paid` коэффициент закупа равен 1 в отчёте по источникам, Cash Flow/PL (включая накопленные остатки), детализациях и аналитике продаж. Исторические сделки без оплаченной покупки сохраняют прежний пересчёт; правило проверяется отдельными тестами.
- Ветка миграции `product-first` завершена (`phase1`-`phase8`), post-migration smoke-check пройден.
- Импорт после удаления `game_titles/game_platforms` считаем успешным; дальнейшие правки только по факту падений.


### Склад собственного каталога
- Подключить автовыдачу селлера к SKU и приоритетам каталога: атомарное резервирование свободного непросроченного ключа, связь с заказом, идемпотентная выдача и освобождение резерва при подтверждённом отказе. Текущие пулы маркетплейсов пока остаются отдельными.
- При росте запаса проверить план `voucher_warehouse_stock` на рабочих объёмах: сейчас сводка считает статусы ключей при чтении, без дополнительного фонового процесса.

## Единый каталог поставщика
- Проверить ближайшие штатные запуски в 09:00 МСК и каждый час после production-релиза 23.09.2026; наблюдать длительность полного обхода и ответы ограничений Интерхаба. На staging опросы цен/остатков, покупки и фоновые задачи отключены; список услуг и баланс читаются онлайн.
- После перехода всех внешних одноразовых импортов Seller убрать старые таблицы `interhub_price_calculations` / `interhub_stock_cache` отдельной согласованной миграцией с резервной копией. Историю покупок не удалять.
- Для подтверждённого массового сокращения каталога (>20% при 10+ позициях) добавить отдельное подтверждение оператора. Сейчас такой ответ целиком отклоняется с видимой ошибкой.
- Уточнить у Интерхаба 10 несовпадений остатков, обнаруженных 23.09.2026: PlayStation KSA / SAR 375, Xbox GBR / GBP 70, два Discord, четыре Jawaker и два Tinder. В `service/detail` отсутствуют точные названия либо есть дубли; до подтверждённого соответствия сохранять последнюю успешную величину, не угадывать по сумме.
- Разобрать ответы `calculate` при первом полном обходе production: семь номиналов PlayStation Lebanon отклоняются с `The account field [nominal] invalid by regular expression`; отдельные Discord, PlayStation France, Xbox GBR и Jawaker возвращают `Unknown error`. Не подменять неизвестную цену нулём и не проверять эти случаи покупкой.
- Перенести прогресс ручного опроса из памяти API в БД, чтобы его можно было читать после рестарта или через другой процесс. Сами опросы уже защищены общей блокировкой PostgreSQL.
- Убрать совместимый путь снимков `VoucherCatalogService` без `shared_catalog` после перевода остальных адаптеров и старых изолированных тестов. В приложении все вызовы используют общий каталог.
- Починить отдельный публичный шлюз staging: `Caddyfile.stage` / staging Compose используют `88.218.61.203`, которого сейчас нет на сервере. Проверочная сборка находится в `/home/adminops/gamesales-staging-preview-20260922`, API/UI доступны на сервере через `127.0.0.1:18081/18080`; локальный UI использует staging-туннель. Production-шлюз не перенастраивался.

- Airpay: добавить в API истории общее число операций и оплаченные суммы по валютам с учётом фильтров. Сейчас UI честно показывает итоги только текущей страницы; Excel выгружает всю отфильтрованную выборку.

- Airpay: фиксированная сумма не гарантирует выдачу ваучера (пример PUBG MOBILE 60 UC DIRECT). До расширения покупок согласовать тип результата по услугам и заменить fixedPayment→voucher отдельным подтверждённым признаком/сопоставлением в CRM и Hub. Диагностический опрос не использует эту эвристику для определения результата.
- Airpay diagnostics: реализованы отдельный журнал и ручной последовательный опрос service/check без pay. Перед выкладкой применить новую миграцию `20260926_01_airpay_diagnostics.sql` к CRM/изолированной схеме Hub и проверить восстановление/конкуренцию на тестовой PostgreSQL. В текущей сессии миграции и реальный опрос не запускались. Автоматическое расписание и заполнение аккаунтов игроков не включены.
