# TODO

## Текущий бэклог
- InterHub: перенести фоновые обновления цен/остатков и их прогресс из памяти процесса в долговечную очередь; текущая защита от повторного запуска действует только внутри одного API worker, а перезапуск теряет job_id.
- InterHub: согласовать явную таблицу соответствий для разных названий одного номинала (например, PNL/PLN и префиксы gf_). В кэше приложения несовпадения и дубли остаются неизвестными, без автоматического угадывания только по цифрам.
- История покупок CRM в таблице: оптимизировать постраничные запросы с повторным подсчётом и поиском последних цен. Выгрузка Excel уже использует отдельное чтение одним запросом; учитывать сохранение поиска и сортировки при переносе оптимизации в таблицу.
- Перед возвратом виджета TR-карты исключить рублёвые покупки ваучеров из списаний с карты в TRY: `_load_finance_card_balance` сейчас вычитает закуп всех завершённых услуг TR без различения способа оплаты.
- Проверить по аудиту старые услуги TR/PL без оплаченных ваучеров, сохранённые после релиза `77b62cd`: фронтенд мог передать закуп 0. Исправление сохранения подготовлено; production-проверку и возможное восстановление сумм из аудита/резервной копии согласовать отдельно, не заменять нулевые суммы массово.
- Безопасность: WebSocket `/ws/deals` передаёт токен в query string, который попадает в access-log Uvicorn. Убрать токены из логов и пересмотреть передачу авторизации; до исправления ограничить доступ к логам и не включать сырые строки WebSocket в диагностику.
- Разделить крупный JS-бандл фронта: `npm run check` предупреждает о чанке больше 500 КБ после минификации; проверить ленивую загрузку разделов WorkView.
- Баланс TR-карты временно скрыт из шапки по запросу пользователя. Когда функционал снова станет актуален, вернуть подключение `WorkTrCardBalanceWidget` в `WorkTopBar`; компонент, расчёты и тесты сохранены.
- Поставщик/сделки: дополнить изолированные API-тесты проверкой настоящих конкурирующих транзакций PostgreSQL (pay, сохранение, удаление, фоновое подтверждение) на отдельной локальной тестовой БД; внешний API поставщика подменять, реальные ваучеры не покупать.
- Добавить пароль и ограничить доступ к Redis (`requirepass`, `bind/network rules`, обновить `REDIS_URL`).
- Добавить серверный поиск/ленивую загрузку аккаунтов в селекторах сделок, чтобы не грузить все аккаунты сразу.
- Добавить частичный индекс активных слотов для ускорения `/accounts/for-deal/availability` (`WHERE released_at IS NULL`).
- Исправить лимит `loadAccountDeals`: пагинация/"Показать еще" или явное уведомление "первые N".
- Защититься от гонки при одновременном создании шеринга: уникальный индекс активного слота (`account_id + slot_type_code WHERE released_at IS NULL`) + обработка 409 на API и сообщение на фронте.
- Добавить CI (GitHub Actions): автоматический запуск `cd gamesales-web && npm run check` на каждый PR.
- Добавить PR-шаблон (`.github/pull_request_template.md`) с чеклистом: комментарии, тесты, локальные проверки.
- Убрать `game-only` ограничения в API аккаунтов для слотов (`/accounts/for-deal`, `/accounts/for-deal/availability`, `/accounts/{id}/products`) и перевести их на поддержку `subscription` без фронтовых fallback-веток.
- Добавить backend-тесты на сценарии шеринга с `subscription` для API аккаунтов (поиск аккаунтов/доступность слотов/привязка товара).
- Для резервов в шеринге: освобождать `reserve_key` при удалении/отмене сделки (сейчас резерв помечается занятым сразу при создании).
- Для резервов в шеринге: добавить ручной выбор/переопределение резерва в форме сделки с серверной валидацией уникальности.
- Централизовать инвалидацию/рефреш клиентских кешей после мутаций (create/update/delete/import): `accountSecrets`, `accountsAll`, `productsAll`, справочники; убрать точечные рассинхроны UI без ручной кнопки "Обновить".
- Для синхронизации Yandex Market: обнулять или отменять старый дневной итог, если при повторной загрузке за диапазон день полностью исчез из доставленных строк отчета.
- Для уведомлений Yandex Market: вывести журнал и причины повторов в интерфейсе владельца; после анализа боевой истории определить политику дедупликации по `event_fingerprint`.
- Для повторной публикации остатка Yandex Market после выдачи: заменить краткоживущий `Timer` на долговечную очередь с повторами.
- Для ручной выдачи ключей Ozon: вынести хранение уже отправленных ключей из plaintext jsonb в отдельное шифруемое хранилище с аудитом доступа до подключения массовых продаж.

## Архив
- Для услуг TR/PL со связанной покупкой InterHub в статусе `paid` коэффициент закупа равен 1 в отчёте по источникам, Cash Flow/PL (включая накопленные остатки), детализациях и аналитике продаж. Исторические сделки без оплаченной покупки сохраняют прежний пересчёт; правило проверяется отдельными тестами.
- Ветка миграции `product-first` завершена (`phase1`-`phase8`), post-migration smoke-check пройден.
- Импорт после удаления `game_titles/game_platforms` считаем успешным; дальнейшие правки только по факту падений.
