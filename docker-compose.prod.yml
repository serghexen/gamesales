services:
  postgres:
    image: postgres:16-alpine
    container_name: gamesales-postgres
    restart: unless-stopped
    env_file: .env.prod
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    mem_limit: 900m
    cpus: "0.80"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 3s
      retries: 30

  api:
    build: ./api
    container_name: gamesales-api
    restart: unless-stopped
    env_file: .env.prod
    environment:
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
      TELEGRAM_API_URL: ${TELEGRAM_API_URL:-http://telegram-service:8082}
      TELEGRAM_API_KEY: ${TELEGRAM_API_KEY}
      UVICORN_WORKERS: ${UVICORN_WORKERS:-1}
      IMPORT_MAX_CONCURRENT_JOBS: ${IMPORT_MAX_CONCURRENT_JOBS:-1}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    mem_limit: 550m
    cpus: "0.90"

  web:
    build:
      context: ./gamesales-web
      args:
        VITE_API_BASE: /api
    container_name: gamesales-web
    restart: unless-stopped
    depends_on:
      - api
    ports:
      - "80:80"
    mem_limit: 120m
    cpus: "0.20"

  queue-api:
    build: ./notify-api
    container_name: gamesales-queue-api
    restart: unless-stopped
    env_file: .env.prod
    environment:
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
      QUEUE_API_KEY: ${QUEUE_API_KEY}
    depends_on:
      redis:
        condition: service_started
    ports:
      - "8081:8081"
    mem_limit: 120m
    cpus: "0.20"

  telegram-service:
    build: ./telegram-service
    container_name: gamesales-telegram-service
    restart: unless-stopped
    env_file: .env.prod
    environment:
      TELEGRAM_API_ID: ${TELEGRAM_API_ID}
      TELEGRAM_API_HASH: ${TELEGRAM_API_HASH}
      TELEGRAM_API_KEY: ${TELEGRAM_API_KEY}
    ports:
      - "8082:8082"
    mem_limit: 220m
    cpus: "0.40"

  redis:
    image: redis:7-alpine
    container_name: gamesales-redis
    restart: unless-stopped
    command: ["redis-server"]
    ports:
      - "6379:6379"
    mem_limit: 120m
    cpus: "0.20"

volumes:
  pgdata:
